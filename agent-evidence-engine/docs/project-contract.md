# Project Contract

The contract is a YAML file that tells the engine:
- What the app is
- How to build it
- What probes to run
- What constitutes failure
- What evidence to collect

See `schema/project-contract.schema.json` for the JSON schema.
See `examples/` for real-world contracts.
