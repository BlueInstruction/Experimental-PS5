{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Evidence",
  "description": "A single piece of structured evidence collected by a probe",
  "type": "object",
  "required": ["type", "key", "value"],
  "properties": {
    "type": {
      "type": "string",
      "description": "Evidence type: file_exists, process_mapped, logcat_line, vulkan_property, etc."
    },
    "key": { "type": "string" },
    "value": {},
    "source": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" }
  }
}
